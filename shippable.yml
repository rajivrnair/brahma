language: ruby
rvm: 2.2.2

bundler_args: --binstubs

before_install:
  - gem install capistrano

before_script:
  - rm -rf shippable/*

script:
  bundle exec rspec

after_success:
  - cap -v production deploy:execute_on_server
language: ruby
rvm: 2.2.2

bundler_args: --binstubs

before_script:
  - rm -rf shippable/*

script:
  bundle exec rspec

after_success:
  - ssh -v deployer@128.199.216.161 'cd brahma && git pull && ./run_me.sh'